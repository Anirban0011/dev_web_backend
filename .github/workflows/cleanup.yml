name: Cleanup PR Environments

on:
  pull_request:
    types: [closed]

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Delete GitHub Environment
        run: |
          gh api -X DELETE \
            /repos/${{ github.repository }}/environments/${{ github.event.ref }}
        env:
          GH_TOKEN: ${{ secrets.WORKFLOW_TOKEN }}